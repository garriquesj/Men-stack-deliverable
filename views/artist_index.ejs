<!DOCTYPE html>
<html>
    <!-- This is my heading now -->
    <%- include ('./partials/head.ejs', ) %>
  <!-- This is my nav bar now -->
  <%- include ('./partials/nav.ejs') %>
    <section class="hero is-medium has-background-light">
      <div class="hero-body">
        <h1 class="title">Artist index Page</h1>
      </div>
    </section>
    <div class="columns" is-info >
  <div class="column">
    First column
  </div>
  <div class="column">
    Second column
  </div>
  <div class="column">
    Third column
  </div>
  <div class="column">
    Fourth column
  </div>
</div>

<main class="container is-max-widescreen">
  <div class="columns is-multiline p-5">
      <% for(let idx = 0; idx < artist.length; idx++){ %> <% const artist =
      artist[idx] %>

  <div class="column is-one-third">
      <article class="card">
          <section class="card-image">
              <figure class="image is-3by3">
                  <img src="<%= artist.image[0] %>" alt="Placeholder image" />
              </figure>
  </div>
  </section>
                  <div class="card-content">
                      <div class="content">
                          <h3>
                          <a href="/artist/<%= artist._id %>"><%= artist.name %> </a>
                          </h3>

                          <p>$<%= artist.numberOfPeices%></p>
                      </div>
                  </div>
              </article>
          </div>
      <% } %>
  </div>
</main>
</body>
</html>
  </body>
</html>